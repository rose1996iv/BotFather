# ── Core ──────────────────────────────────────────────────────────────────────
Flask==3.1.3
groq==1.2.0
gunicorn==23.0.0
python-dotenv==1.2.2
requests==2.33.1

# ── BM25 retrieval (replaces sentence-transformers — saves ~400 MB RAM) ────────
rank-bm25==0.2.2

# NOTE: python-telegram-bot and duckduckgo-search are intentionally removed.
# Telegram uses webhook via plain requests (no polling thread, no signal handler issues).
# Web search uses stdlib urllib (no extra library needed).
